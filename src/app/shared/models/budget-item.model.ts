export class budgetItem {
    description: string;
    amount: number;

    constructor(description: string, amount: number) {
        description = this.description;
        amount = this.amount;
    }
}